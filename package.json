
{
  "name": "purchase-master",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel-optimized purchase management software",
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p dist && cp -r components pages lib utils types.ts index.html index.tsx main.js vercel.json metadata.json dist/ 2>/dev/null || true"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
